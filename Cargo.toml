[profile.release]
strip = true
lto = true
panic = "abort"
opt-level = 0

[profile.dev]
strip = true
# opt-level = 1

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace]
resolver = "2"
members = ["client", "common", "package", "server"]
